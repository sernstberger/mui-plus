{
  "name": "mui-plus",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/mui-plus",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/mui-plus",
        "outputPath": "dist/apps/mui-plus"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/mui-plus"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "mui-plus:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "mui-plus:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "mui-plus:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "mui-plus:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/mui-plus/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/mui-plus/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
